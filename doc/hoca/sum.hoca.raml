type nat = Zero | S of nat;;

let rec fold f z xs =
  match xs with
  | [] -> z
  | x::xs' -> f x (fold f z xs')
in
let rec plus x y =
  match x with
  | Zero -> y
  | S(x') -> S(plus x' y)
in fun l -> fold plus Zero l;;


